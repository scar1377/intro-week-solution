

const item0 = 'apples';
const item1 = 'bananas';
const item2 = 'pears';

let shoppingList = item0.concat(FILL_ME_IN);

// use the additional item variables from above
// checkThat(shoppingList).isEqualTo('apples, bananas and pears');


const filePath1 = '/Users/mitch/northcoders/remote_precourse/day_1/1_intro_strings';

// find the index position of the last / forward slash
// use a string method to find the slice of the string at that position
// check that a value can 
















var FILL_ME_IN;