function makePoint(up,down) {
  return {
    up: up,
    down: down
  }
}

// Create an object with a point up and a point property down

function goUp(point,forwardAmount) {

}
// Should take a point object and increment the up property value by a specified amount
// The function should return the new point object with the incremented point property

const point = makePoint(10,10);
goUp(point,10); // should return {      }